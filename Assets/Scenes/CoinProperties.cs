// CoinProperties.cs
using UnityEngine;

public class CoinProperties : MonoBehaviour
{
    // This script simply acts as a tag to identify GameObjects as Carrom coins (or Queen).
    // You can add properties here later if needed, e.g., int scoreValue;
}